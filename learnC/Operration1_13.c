#include <stdio.h>
/*Exercice 1 */
int main()
{
	/*char c = '\x01';
	int cd = (int)c;
	short p = 10;
	int cp = (int)p;

	p += 3;
	printf("I think is 13 the real answer : %d\n", p);
	c += 1;
	printf("le resulta sera de plus un %d \n", c);
	cp += cd;
	printf("le resultat est de 11 le vrai resulta %d", cp); */

	/*Exercice 2 */

	/*char c = '\x05';
	int cd = (int)c;
	int n = 5;
	long p = 1000;
	int cp = (int)p;
	float x = 1.25;

	int cx = (int)c;
	double z = 5.5;

	n += cd += cp;
	printf("le resulta est 11 le vrai et %d\n", n);
	cx *= 2 + cp;
	printf("le resultat est 1000 et la vrai reponse est %d\n", cx);*/

	/* Exercice 3*/
	/*int nb;
	printf("Enter a integer : ");
	scanf("%d", &nb);

	if (nb != 0)
	{
	int negatife;
	negatife = -nb;
	printf("The value number but negatif %d \n",negatife);
	float half;
	half = nb /2;
	printf("The half of the value is %f\n", half);
	} */
	/* Exercice 4 */
	//Les etaps de la programmation
	/*
	1. tu multi le franc par 6.55957
	*/

	/*printf("_|__________convertisseur francs/euros__________|_\n");
	printf("Entre un nombre : ");
	float franc;
	scanf("%f", &franc);
	float euro;
	euro = 0.0015;
	franc *= euro;
	printf("Votre argent en euro %.2f", franc); */
	/* Exercice 5*/
	/*printf("Convertit en degrés Celsius | En degrés Fahrenheit\n");
	float user;
	float f;
	printf("Entre une temperature : ");
	scanf("%f", &user);
	f = (((9 * user) / 5) + 32);
	printf(" la temperature en Fahrenheist %.2f",f);
	*/
	/* Exercice 6 */
}
