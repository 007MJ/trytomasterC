#include <stdio.h>

int main(void)
{
	int a =10, b =15;
	printf("%d",a < b);
	printf("%d", a + b > 12);
	char *s = NULL;
	if (!s)
	{
		printf("C'est commet tu pense !");
	}
	else
		printf("c'est pas comme tu pense");

}
